import type { Meta, StoryObj } from '@storybook/react';
import EateryDetail from './EateryDetail';

const meta: Meta<typeof EateryDetail> = {
    component: EateryDetail,
};

export default meta;
type Story = StoryObj<typeof EateryDetail>;

export const Primary: Story = {
};
